#ifndef MACRO_UTILS_H
#define MACRO_UTILS_H

/**
 * @brief uma abreviação para o padrão de iteração que costumo usar
 */
#define FOR_EACH(expression) for (auto &it : expression)

#endif // MACRO_UTILS_H
